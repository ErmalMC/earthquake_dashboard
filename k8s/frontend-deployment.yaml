apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: earthquake
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: ermalmc/earthquake-frontend:latest
          ports:
            - containerPort: 80
